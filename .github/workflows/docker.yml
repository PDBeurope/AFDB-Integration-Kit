name: Build

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  valid_tag:
    if: github.ref_type == 'tag'
    runs-on: ubuntu-latest
    steps:
    - name: Check that the tag follows semantic versioning (must not start with "v")
      run: echo ${{ github.ref_name }} | egrep '^[0-9]+\.[0-9]+\.[0-9]+\S*$'

  docker_build_and_push_to_dockerhub:
    needs: [valid_tag]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          push: true
          platforms: linux/amd64,linux/arm64/v8
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/afdb-integration-toolkit:${{ github.ref_name }}
            ${{ secrets.DOCKER_USERNAME }}/afdb-integration-toolkit:latest
